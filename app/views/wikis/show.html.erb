<h1><%= markdown @wiki.title %></h1>
<%= markdown @wiki.body %>

<%= link_to "Edit", edit_wiki_path, class: 'btn btn-success' %>
<p><%= button_to "Delete Wiki", wiki_path(@wiki), data: { confirm: "Are you sure?" }, method: :delete, class: 'btn btn-danger' %></p>
